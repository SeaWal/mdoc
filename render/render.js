const textEditor = document.querySelector(".text-editor");
const preview = document.querySelector(".markdown-preview");

textEditor.addEventListener("keyup", evt => {
    textEditor.innerText.add("hshjfh")
})